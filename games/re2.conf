# Resident Evil 2 Remake (2019) - RE Engine
# Status: untested (pre-built DLL included)
# PCGamingWiki confirms this game has cutscene playback issues related to
# Windows Media Foundation -- the same class of bug this fix addresses.
# Directory name is best-effort based on Capcom's naming convention.
# If install fails, check the actual folder name in steamapps/common/.

GAME_ID="re2"
GAME_NAME="Resident Evil 2"
GAME_DIR_NAME="RESIDENT EVIL 2 BIOHAZARD RE2"
GAME_EXE="re2.exe"
STEAM_APP_ID="883710"
GAME_PREFIX="re2"
PLIST_LABEL="com.re2fix.decode-server"
STATUS="untested"

BOTTLE_ENV_VARS=()
DISABLE_CRASH_REPORTER=false
